package com.spectalk.app.ui.theme

import androidx.compose.ui.graphics.Color

// ── Gervis Brand Colors ───────────────────────────────────────────────────────
// Derived from the Gervis mascot: metallic red microphone body + gold capsule.

// Metallic Red — primary (mic body, deep rich red with metallic warmth)
val GervisRed10  = Color(0xFF3B0000)
val GervisRed20  = Color(0xFF5F0000)
val GervisRed30  = Color(0xFF8B0000)
val GervisRed40  = Color(0xFFB71C1C)   // Core brand red
val GervisRed50  = Color(0xFFC62828)   // Metallic highlight
val GervisRed80  = Color(0xFFEF9A9A)   // Tinted red for dark surfaces
val GervisRed90  = Color(0xFFFFCDD2)
val GervisRed95  = Color(0xFFFFEBEE)

// Metallic Gold — secondary (capsule, glasses accents, shimmer)
val GervisGold10 = Color(0xFF2C1A00)
val GervisGold20 = Color(0xFF4A2E00)
val GervisGold30 = Color(0xFF6B4300)
val GervisGold40 = Color(0xFF9A6B00)
val GervisGold50 = Color(0xFFC89A00)   // Core brand gold
val GervisGold60 = Color(0xFFD4A80E)   // Warm metallic shimmer
val GervisGold80 = Color(0xFFFFD966)   // Bright gold for dark surfaces
val GervisGold90 = Color(0xFFFFEFA0)
val GervisGold95 = Color(0xFFFFFBE6)

// ── Surface / Neutral — warm-toned dark backgrounds ───────────────────────────
val Obsidian  = Color(0xFF0C0808)   // Deepest bg (dark theme)
val Carbon    = Color(0xFF1A1212)   // Card / surface dark
val Charcoal  = Color(0xFF2C1F1F)   // Surface variant dark
val Ash       = Color(0xFF3D2E2E)   // Outline / divider dark

val Cream     = Color(0xFFFFF8F5)   // Background light
val Linen     = Color(0xFFFFF0EB)   // Surface light
val Silk      = Color(0xFFEDE0DC)   // Surface variant light
val Stone     = Color(0xFF9E8986)   // Outline light
